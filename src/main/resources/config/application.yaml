logging:
    level:
        root: WARN
        fr.kuremento.dnd: DEBUG
        org.springframework.context.support: ERROR

spring:
    application:
        name: '@project.name@'
    jpa:
        hibernate:
            ddl-auto: none
    main:
        allow-bean-definition-overriding: true
server:
    port: 8080

application:
    file:
        output-file: "classpath:input-français.pdf"
    version: '@project.version@'